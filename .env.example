# SmartTeacher Environment Variables (optional — the app works fully offline without any of these)

# Supabase (client-side + optional cloud sync mode)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
